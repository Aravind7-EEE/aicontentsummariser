PORT=5000
MONGO_URI=YOUR_MONGODB_CONNECTION_STRING
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Do NOT commit actual secrets to the repository. Copy this file to `.env`
# and set real values locally or in your deployment provider's environment
# variable settings (e.g., Vercel Environment Variables).
